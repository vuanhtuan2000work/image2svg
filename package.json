{
  "name": "image2svg-cloudflare",
  "private": true,
  "type": "module",
  "description": "Optional Cloudflare Worker deploy for the image2svg convert UI",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "cf:dev": "wrangler dev"
  },
  "dependencies": {
    "fflate": "^0.8.3",
    "upng-js": "^2.1.0"
  },
  "devDependencies": {
    "wrangler": "^4.102.0"
  }
}
