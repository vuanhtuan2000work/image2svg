# SEO + star-growth checklist (post-README)

README is rewritten as a conversion landing page. Do these outside the file for max discovery:

## GitHub About (required)

**Description (paste):**

```text
Offline PNG/JPEG to SVG converter — local raster-to-vector CLI + web UI (vtracer). Batch image to SVG for game assets.
```

**Website:** `https://github.com/vuanhtuan2000work/image2svg#readme`

**Topics (add all):**

`svg` `png-to-svg` `image-to-svg` `raster-to-vector` `vtracer` `vectorize` `offline` `self-hosted` `python` `fastapi` `game-assets` `batch-processing`

## This week distribution (highest ROI)

1. **Show HN / Reddit** — story angle: “I needed offline PNG→SVG for game assets without uploading art to SaaS”
2. **Awesome lists** — PR into lists for SVG, Python graphics, game tools
3. **X/Discord** — short clip: Web UI drag PNG → SVG in 10s
4. **Dev.to / Hashnode** — “Local PNG to SVG with Python + vtracer”
5. Pin the repo and enable **Discussions** (Q&A = long-tail SEO)

## Demo upgrade (next)

- Add `docs/demo/before-after.gif` (PNG → SVG) and put it under the hero image in README
- Optional: GitHub Release `v0.3.0` with install notes (badges + trust)
