vtracer==0.6.*
pyyaml>=6.0
scour>=0.38
pillow>=10.0
pillow-heif>=1.4.0
fastapi>=0.115
uvicorn[standard]>=0.32
python-multipart>=0.0.9
svg.path>=6.0
numpy>=1.26
pulp>=2.8
# Optional remove-bg chain:
# - BiRefNet self-host: IMAGE2SVG_BIREFNET_URL is an engine endpoint, not an image URL.
# - BEN2 Base: uses cached/local weights; set IMAGE2SVG_BEN2_ALLOW_DOWNLOAD=1 to fetch on demand.
# - rembg CPU: default local detector prefers birefnet-general, then isnet-general-use, then u2net.
# Optional (needs system cairo): cairosvg>=2.7
# Optional raster CLI: librsvg (rsvg-convert)
